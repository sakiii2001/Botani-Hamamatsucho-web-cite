{
  "installCommand": "pnpm install --frozen-lockfile --config.auto-install-peers=true",
  "buildCommand": "pnpm run build",
  "build": {
    "env": {
      "LIGHTNINGCSS_FORCE_WASM": "1"
    }
  },
  "env": {
    "LIGHTNINGCSS_FORCE_WASM": "1"
  }
}
